Le schema du code est le MVC.  Je crée les objets en fonction des tables que j'ai dans la base de données, ici en l'ocurrence, 
dans le dossier objet, le dossier manager est le model dans lequel j'effectue les actions et les requêtes vers la base de données
et le controlleur se trouve dans le dossier fichiersAjax qui récupère les actions choisies par l'utilisateur dans les vues.
